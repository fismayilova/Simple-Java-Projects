/* Task04 * Implement a String method public static String birthdayInfo(int bday)  *  which takes a full birthdate of someone as integer parameter (in the YYYYMMDD format) and  *  returns String info about that person, such as whether his birth-year is Leap Year (when Feb is 29 days)  *  and his Zodiac Sign.  *   *  For example,println( birthdayInfo(20040410) ) "Leap Year. Zodiac is Aries."println( birthdayInfo(20031118) ) "Not Leap Year. Zodiac is Scorpio." * Note that the number will always be a correct integer. No need to check errors. *  * For info about Zodiac Signs go to http://www.psychicguild.com/horoscopes_explained.php    */public class Task05 {	public static void main(String[] args) {		/*		 * No need to modify main method. Just make sure your method satisfies		 * ALL possible test cases according to task instructions, such as		 * below.		 */		System.out.println(birthdayInfo(20040210)); // "Leap Year. Zodiac is													// Aries."		System.out.println(birthdayInfo(20031118)); // "Not Leap Year. Zodiac is													// Scorpio."	}	/**	 * Returns info about a person birthdate	 * 	 * @param bday	 *            Full birthdate of a person	 * @return String info about the birthdate	 */	private static String birthdayInfo(int bday) {		String leapYear = "";		String notLeapYear = "";		// as date of birth goes as YYYYMMDD to reach the year we divide bday by		// 10000		if ((bday / 10000) % 4 == 0) { // if year is leap it should be divisible										// by 4			leapYear = "Leap Year. " + checkTheZodiac(bday);			return leapYear;		} else {			notLeapYear = "Not Leap Year. " + checkTheZodiac(bday);			return notLeapYear;		}	}	/**	 * Returns info about a person Zodiac	 * 	 * @param bday	 *            Full birthdate of a person	 * @return String info about the Zodiac considering months and days	 */	private static String checkTheZodiac(int bday) {		int month = (bday / 100) % 100; // we divide by 100 to remove days and %										// 100 to get the reminder AKA month		int day = (bday % 100); // we use % 100 to get reminder of whole number								// AKA days		int otherMonths = (bday / 100) % 1000; // as i couldn't write 08 and 09												// i												// used %1000 instead of 100		String wrongDate = "Wrong Date!"; // if wrong date is written it gives											// default output		// checking the months and days to find zodiac.		if (day >= 21 && (month) == 03 || day <= 20 && (month) == 04) {			String Aries = "Zodiac is Aries";			return Aries;		} else if (day >= 21 && (month) == 04 || day <= 21 && (month) == 05) {			String Taurus = "Zodiac is Taurus";			return Taurus;		} else if (day >= 22 && (month) == 05 || day <= 21 && (month) == 06) {			String Gemini = "Zodiac is Gemini";			return Gemini;		} else if (day >= 22 && (month) == 06 || day <= 22 && (month) == 07) {			String Cancer = "Zodiac is Cancer";			return Cancer;		} else if (day >= 23 && (month) == 07 || day <= 22 && (otherMonths) == 8) {			String Leo = "Zodiac is Leo";			return Leo;		} else if (day >= 23 && (otherMonths) == 8 || day <= 23 && (otherMonths) == 9) {			String Virgo = "Zodiac is Virgo";			return Virgo;		} else if (day >= 24 && (otherMonths) == 9 || day <= 23 && (month) == 10) {			String Libra = "Zodiac is Libra";			return Libra;		} else if (day >= 24 && (month) == 10 || day <= 22 && (month) == 11) {			String Scorpio = "Zodiac is Scorpio";			return Scorpio;		} else if (day >= 23 && (month) == 11 || day <= 21 && (month) == 12) {			String Sagittarius = "Zodiac is Sagittarius";			return Sagittarius;		} else if (day >= 22 && (month) == 12 || day <= 20 && (month) == 01) {			String Capricorn = "Zodiac is Capricorn";			return Capricorn;		} else if (day >= 21 && (month) == 01 || day <= 19 && (month) == 02) {			String Aquarius = "Zodiac is Aquarius";			return Aquarius;		} else if (day >= 20 && (month) == 02 || day <= 20 && (month) == 03) {			String Pisces = "Zodiac is Pisces";			return Pisces;		}		return wrongDate;	}}